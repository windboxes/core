import { processRepeatStyle } from '../src';

const sourceStyles = ['mt-10', 'mt-5', 'p-4', 'bg-teal-100', 'bg-teal-200', 'rounded'];

console.log('sourceStyles', sourceStyles);
console.log('processRepeatStyle', processRepeatStyle(sourceStyles));
